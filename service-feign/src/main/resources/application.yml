eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8765

spring:
  application:
    name: service-feign

# 打开Feign的自带断路器
feign:
  hystrix:
    enabled: true